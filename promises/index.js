/*
const example1 = {
	exampleMethod() => {
		return new Promise((resolve, reject) => {
			rmsClient.getProgrammes().then((res)=>resolve(res)).catch((err)=>reject(err));
		})
	}
}

const example2 = {
	exampleMethod() => {
		return new Promise(async (resolve, reject) => {
      try {
        const prog = await rmsClient.getProgrammes();
        resolve(prog);
      } catch(e) {
        reject(e);
      }
		})
	}
}
*/
